# 2026/06/21

## 今日やったこと

- len()を学んだ
- split()を使って入力をリストに変換した
- カンマ区切りで入力された点数を処理するプログラムを作成した
- 合計点と平均点を求められるようにした
- 最高点を求める機能を追加した

- defを用いて計算ツールをまとめた

## 気づいたこと

- len()はリストの要素数だけでなく、文字列の文字数も取得できる。

- split()で作られたリストの中身は文字列(str)なので、
そのままでは計算できず、int()で数値に変換する必要がある。

- defで作った関数は変数として使うこともできる。
- 

## 学んだこと

コンマで区切ったリストを入力したときにsplit(",")で分割する必要があった。

また、最高点や80点以上の人数などは、別々のfor文を書くのではなく、同じfor文の中でまとめて計算したほうがスムーズになると分かった。

(関数名)のみと(関数名)(引数)では全く異なるものを指していた。関数名のみだと、関数による計算結果ではなく関数そのものがオブジェクトとして返してしまうと分かった。


## 次回やりたいこと

- listとdefを組み合わせる(済)
- while文を用いて、同じリストのものを「もう一度計算する」仕組みを実装する
- Arduino IDEの導入と基礎学習

## そのうちやりたいこと
- 公式LINEのプログラムを理解した状態で書いて、多数の対応になっても早い応答ができるようにしたい